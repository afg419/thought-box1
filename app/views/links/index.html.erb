<h1>All dem Links for <%= current_user.email %> </h1>

<div class="container">
  <%= form_for @link do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %> <span>|||</span>
  <%= f.label :url %>
  <%= f.text_field :url %><br>
  <%= f.submit "Create new link" %>

  <% end %>
</div>

<!-- <div class="container">
  <ul>
    <% @links.each do |link| %>
    <li>
      <h4><%= link.title %></h4>
      <p><%= link.url %></p>
      <p class=<%=link.read%> >Read: <%= link.read %></p>
      <button type="button" name="button">Mark as Read</button>
    </li>
    <% end %>
  </ul>
</div> -->


<%= react_component 'Main' %>
